{
  "scripts": {
    "start": "concurrently --raw -n backend,frontend -c green,blue \"npm run  dev --prefix Backend\" \"npm run dev --prefix Frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
